# Lote Pro - dependências do backend
# IMPORTANTE: NÃO usar fiona (sem wheel para Python 3.14 no Windows).
# A leitura/escrita de vetores (SHP/KML/GeoJSON) é feita via pyogrio (GDAL embutido).

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Geoprocessamento (núcleo)
geopandas>=1.1
shapely>=2.1
pyproj>=3.7
pyogrio>=0.10          # engine de I/O vetorial (substitui fiona)

# Dados REAIS via OpenStreetMap (sem chave): geocodificação (Nominatim),
# footprints de edificações e malha viária (Overpass).
osmnx>=2.1

# Fonte opcional de footprints de alta cobertura: Overture Maps (open data),
# consultada por bbox via DuckDB (extensões spatial + httpfs, sem chave).
duckdb>=1.3

# Exportação
simplekml>=1.3         # export KML
openpyxl>=3.1          # export Excel (.xlsx)

# Provider opcional Google Earth Engine (NÃO instalado por padrão).
# Descomente para habilitar o provider espectral NDVI/NDBI:
# earthengine-api>=1.0
